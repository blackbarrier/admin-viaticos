{{ form_start(form, {'attr': {'id': 'form-parametro', 'class': 'form-validate', 'novalidate': '' , 'autocomplete':'off'}}) }}
{{ form_row(form._token) }}

<div class="row">
    <div class="col-lg-3">
        <div class="form-group">
            {{ form_label(form.nombre) }}
            {{ form_widget(form.nombre) }}
        </div>
    </div>
    <div class="col-lg-3">
        <div class="form-group">
            {{ form_label(form.valor) }}
            {{ form_widget(form.valor) }}
        </div>
    </div>    
</div>
<div class="row">
    <div class="col-sm-12">
        <div class="form-group">
            {{ form_label(form.leyenda) }}
            {{ form_widget(form.leyenda) }}
        </div>
    </div>
</div>

<hr/>
<div class="row text-center">
    <div class="col-sm-6 col-md-4 col-lg-2 mb-3">
        <button type="submit" class="btn btn-inverse btn-inverse-jag btn-outline-inverse">
            <i class="ti-save"></i> Guardar</button>
    </div>    

</div>
{{ form_end(form) }}
