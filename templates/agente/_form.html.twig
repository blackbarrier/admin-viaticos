{{ form_start(form) }}


<div class="form-group row">
    <div class="col col-sm-4">
        {# <input type="text" class="form-control" placeholder="col"> #}
        {{ form_label(form.nombre) }}
        {{ form_widget(form.nombre) }}
        
    </div>
    <div class="col col-sm-4">
        {# <input type="text" class="form-control" placeholder="col-sm-4"> #}
        {{ form_label(form.apellido) }}
        {{ form_widget(form.apellido) }}
    </div>
    <div class="col">
        {{ form_label(form.legajo) }}        
        {{ form_widget(form.legajo) }}
    </div>
    <div class="col">
        {{ form_label(form.numeroDocumento) }}
        {{ form_widget(form.numeroDocumento) }}
    </div>
</div>
<div class="form-group row">
    <div class="col">
        {{form_label(form.cuil) }}
        {{form_widget(form.cuil) }}
    </div>
    <div class="col-sm-4">
        {{form_label(form.genero) }}
       {{ form_widget(form.genero) }}
    </div>
    <div class="col">
        {{form_label(form.categoria) }}
        {{ form_widget(form.categoria) }}
    </div>
    {# <div class="col">
        {{form_label(form.externo) }}
        {{ form_widget(form.externo) }}
    </div> #}
    
</div>
   {% set arrayRoles = app.user.fetchRoles() %}
   {% if 'ROLE_ADMIN' in arrayRoles %}
   <label class="required">Dependencia</label>
    <div class="form-group row">
        {# <div class="col" id="col_reparticion">

           {% if dependencias %}
            <select class="form-control select2" name="reparticiones" id="reparticiones">
                <option disabled selected value>Seleccione una repartición</option>
                {% for reparticion in reparticiones %}
                    <option value="{{reparticion.id}}" >{{reparticion.nombre}}</option>
                {% endfor %}
            {% endif %}
            </select>

        </div> #}
        <div class="col">


            <select class="form-control select2" name="dependencias" id="dependencias">
            <option disabled selected value> Seleccione una dependencia</option>
                {% for dependencia in dependencias %}
                    {% if dependenciaAgente %}
                        {% if dependenciaAgente == dependencia.id %}
                            <option value="{{dependencia.id}}" selected>{{dependencia.nombre}}</option>
                        {% else %}
                            <option value="{{dependencia.id}}" >{{dependencia.nombre}}</option>
                        {% endif %}
                    {% else %}
                        <option value="{{dependencia.id}}">{{dependencia.nombre}}</option>
                    {% endif %}
                {% endfor %}
            </select>
           
        </div>
        
    </div>
    {% endif %}
<br><br>
<div class="form-group row">
    <div class="col">
         <button class="btn btn-inverse btn-inverse-jag btn-outline-inverse" id="agente_Guardar"><i class="ti-save"></i>&nbsp;{{ button_label|default('Guardar') }}</button>
    </div>
   
</div>
    
        
        
{{ form_end(form) }}
