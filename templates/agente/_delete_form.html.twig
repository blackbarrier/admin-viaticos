<form method="post" action="{{ path('app_agente_delete', {'id': agente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agente.id) }}">
    <button class="btn btn-inverse btn-inverse-jag btn-outline-inverse">Delete</button>
</form>
