<form method="post" action="{{ path('app_feriado_delete', {'id': feriado.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ feriado.id) }}">
    <button class="btn">Delete</button>
</form>
