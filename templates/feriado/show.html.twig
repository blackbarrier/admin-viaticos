{% extends 'base.html.twig' %}

{% block title %}Feriado{% endblock %}

{% block body %}
    <h1>Feriado</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ feriado.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ feriado.descripcion }}</td>
            </tr>
            <tr>
                <th>Mes</th>
                <td>{{ feriado.mes }}</td>
            </tr>
            <tr>
                <th>Anio</th>
                <td>{{ feriado.anio }}</td>
            </tr>
            <tr>
                <th>Dia</th>
                <td>{{ feriado.dia }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_feriado_index') }}">back to list</a>

    <a href="{{ path('app_feriado_edit', {'id': feriado.id}) }}">edit</a>

    {{ include('feriado/_delete_form.html.twig') }}
{% endblock %}
