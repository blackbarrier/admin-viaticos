{{ form_start(form) }}
    <div class="form-group row">
        <div class="col col-sm-4">            
            {{ form_label(form.nombre) }}
            {{ form_widget(form.nombre) }}        
        </div>
        <div class="col col-sm-4">            
            {{ form_label(form.abreviatura) }}
            {{ form_widget(form.abreviatura) }}        
        </div>
    </div>

    <div class="form-group row">
        <div class="col col-sm-4">            
            {{ form_label(form.cuit) }}
            {{ form_widget(form.cuit) }}        
        </div>
        {# <div class="col col-sm-4">            
            {{ form_label(form.rotulo) }}
            {{ form_widget(form.rotulo) }}        
        </div> #}
        {# <div class="col col-sm-4">            
            {{ form_label(form.sucursal) }}
            {{ form_widget(form.sucursal) }}        
        </div>     #}
    </div>
    <div class="form-group row">
        <div class="col col-sm-4">            
            {{ form_label(form.numeroCuenta) }}
            {{ form_widget(form.numeroCuenta) }}        
        </div>
        {# <div class="col col-sm-4">            
            {{ form_label(form.digito) }}
            {{ form_widget(form.digito) }}        
        </div> #}
        <div class="col col-sm-4">            
            {{ form_label(form.cbu) }}
            {{ form_widget(form.cbu) }}        
        </div>
    </div>

    <br><br>
    <div class="form-group row">
        <div class="col">
            <button class="btn btn-inverse btn-inverse-jag btn-outline-inverse"><i class="ti-save"></i>{{ button_label|default('Guardar') }}</button>            
        </div>    
    </div>

{{ form_end(form) }}
