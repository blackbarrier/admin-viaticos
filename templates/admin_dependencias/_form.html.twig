{{ form_start(form) }}
    <div class="form-group row">
        <div class="col col-sm-4">            
            {{ form_label(form.nombre) }}
            {{ form_widget(form.nombre) }}        
        </div>
        <div class="col col-sm-4">            
            {{ form_label(form.abreviatura) }}
            {{ form_widget(form.abreviatura) }}        
        </div>
        <div class="col col-sm-4">            
            {{ form_label(form.descripcion) }}
            {{ form_widget(form.descripcion) }}        
        </div>
    </div>

    <br><br>
    <div class="form-group row">
        <div class="col">
            <button class="btn btn-inverse btn-inverse-jag btn-outline-inverse"><i class="ti-save"></i>{{ button_label|default('Guardar') }}</button>            
        </div>    
    </div>

{{ form_end(form) }}
